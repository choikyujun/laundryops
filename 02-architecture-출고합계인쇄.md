# 개발 핸드오프 — 출고 대조 화면 재배치 + 월 누계 인쇄

> CEgo개발팀 개발 준비 단계. Claude Code에 넘기기 전 기술 기반.
> 대상: LaundryOps v38 (웹, vanilla JS + Supabase). 기준: `01-prd-출고합계인쇄.md`, `STATUS-출고합계인쇄.md`
> 승인일: 2026-09-11

---

## 1. 타당성 점검

- **가능한가:** 가능. 기존 `outbound-compare.js` 내부 재배치 + 팝업 1개 + 인쇄 경로. **신규 테이블·컬럼 없음. SQL 없음.**
- **막히는 것:** 없음. 외부 API 불필요.
- **위험:** 인쇄 CSS가 열 위치에 의존하는 기존 코드와 충돌 가능. 태스크 0에서 확인 후 결정.
- **결론: 진행.**

## 2. 아키텍처 결정

**파일**
- `features/outbound-compare.js` — 재배치 + 팝업 + 인쇄. 이 기능 전부
- `index.html` — 팝업 컨테이너 추가(필요 시), 캐시 태그
- **신규 파일 없음.** 같은 섹션의 일이므로 파일을 쪼개면 상태 공유가 번거로워진다

**함수 분리**

현재 `_buildHTML()`이 ① 월 누계 ② 일자별을 한 번에 만들어 `section.innerHTML`로 교체한다. 이를 나눈다.

- `_buildSummaryHTML(ym)` — 월 누계 대조표. **인자로 월(YYYY-MM)을 받아** 팝업이 다른 월을 그릴 수 있게 한다
- `_buildDailyHTML()` — 일자별 내역. 섹션의 현행 월 사용
- 섹션은 `_buildDailyHTML()`만 렌더, 팝업은 `_buildSummaryHTML(팝업월)`만 렌더

**상태 격리 (가장 중요)**

- 섹션의 현재 월: 기존 변수 그대로
- 팝업의 월: **팝업 전용 변수.** 팝업 열 때 섹션 월로 초기화하고, 닫으면 버린다
- **두 변수를 절대 공유하지 말 것.** 공유하면 팝업에서 월을 바꿀 때 뒤 화면과 버튼 이름이 따라 바뀐다 (PRD 3-3 위반)

**누계 계산 재사용**

태스크 7(`invoice_id` 기준 짝짓기)에서 정리한 **"출고가 묶인 명세서" 단위 로직을 그대로 재사용**한다. 월만 인자로 바꿔 호출. 새로 짜면 두 벌이 되어 숫자가 어긋난다.

- 묶인 출고는 날짜 범위를 걸지 않는다 (월말 출고가 다음 달 1일 명세서에 묶이는 경우 누락 방지)
- 세탁 대기(`invoice_id is null`) 출고만 월 범위 + `outbound_start_date` 적용
- 한쪽만 있는 건(세탁 대기 / 출고 미입력)은 누계 합산에서 제외

**금지 (기존 v38 규칙)**
- `app_v38.js` 직접 수정
- `saveData()` / `fetchFromSupabase()` / `syncToSupabase()` / `platformData.*` / `f.hotels[]`
- 모든 접근은 `window.mySupabase.from(...)`

## 3. 태스크 분해 (한 번에 하나씩)

**0. 사전 확인 (코드만, 변경 금지)**
- `print-fix.js`가 `printReport()`를 어떻게 오버라이드하는지, 인쇄 CSS가 어느 선택자에 의존하는지
- 대조표 열 구성에 그대로 쓸 수 있는지, 아니면 별도 인쇄 경로가 나은지 — **의견 포함해 보고**
- 앱 내 기존 팝업/모달 패턴 (마크업·열고닫기 방식). **새로 발명하지 말고 기존 것 재사용**
- `_buildHTML()`의 현재 구조와 월 변수명
- "오늘 출고 입력" 버튼이 있는 행의 마크업 (같은 행에 버튼 추가 가능한지)
- 보고 후 멈춤

**1. 함수 분리 + 재배치**
`_buildSummaryHTML(ym)` / `_buildDailyHTML()` 분리. 섹션에는 일자별만 남긴다.
→ 누계 숫자가 이동 전과 같은지 확인.

**2. 월 합계 버튼**
출고 입력 버튼과 같은 행 오른쪽 끝. 이름 `M월 합계`(현재 월 동적).
→ 폰 좁은 폭에서 줄바꿈되지 않는지 확인.

**3. 팝업**
기존 모달 패턴 재사용. 월 선택(◀ ▶). **팝업 전용 월 상태.**
→ 팝업에서 월을 바꿔도 뒤 화면·버튼 이름 불변 확인.

**4. 인쇄**
태스크 0 결론에 따라 기존 경로 재사용 또는 별도 경로.
머리말(거래처명·대상 월·출력일). 상태 표시·색 규칙 유지.

**5. 자기 점검 + 배포**
`01-prd-출고합계인쇄.md` 완료 정의 대조 표 보고 → 캐시 태그 갱신 → push.

## 4. 앱 CLAUDE.md (규칙 — 기존에 병합)

- **기준 문서 우선:** `01-prd-출고합계인쇄.md` 범위 밖 기능 추가 금지. non-goals(일자별 인쇄, 엑셀, 월 동기화, 공장 화면)를 되살리지 말 것
- **v38 하드 룰:** `app_v38.js` 직접 수정 금지. `features/` + `window.*` 오버라이드로만
- **SQL-First:** 이 기능은 신규 SQL이 없다. 기존 조회만 재사용
- **이모지 금지**, 절제 미니멀 톤
- **배포:** 캐시 태그(`?v=YYYYMMDD_XX`) 갱신 → `git push` → Vercel 자동 배포
- **결정 기록:** 변경 시 `STATUS-출고합계인쇄.md` 결정 로그에 "무엇을·왜"

## 5. 킥오프 프롬프트

```
01-prd-출고합계인쇄.md, 02-architecture-출고합계인쇄.md, CLAUDE.md를 읽어줘.
LaundryOps v38. app_v38.js 직접 수정 금지, features/ 오버라이드로만.

먼저 태스크 0(사전 확인)만:
- print-fix.js의 printReport 오버라이드 구조와 인쇄 CSS 의존 선택자
- 대조표에 재사용 가능한지, 별도 인쇄 경로가 나은지 의견 포함
- 앱 내 기존 모달 패턴 (재사용할 것, 새로 발명 금지)
- outbound-compare.js _buildHTML() 구조와 월 변수명
- "오늘 출고 입력" 버튼 행 마크업

코드는 바꾸지 말고 확인 결과만 보고하고 멈춰줘.
이후 태스크는 목록 순서대로 하나씩, 끝낼 때마다 멈추고 보고.
```

## 결정 로그 (설계 단계)

| 결정 | 이유 |
|------|------|
| 신규 파일 없이 `outbound-compare.js` 안에서 | 같은 섹션의 일. 쪼개면 상태 공유가 번거로움 |
| `_buildSummaryHTML(ym)`이 월을 인자로 받음 | 팝업이 다른 월을 그려야 함 |
| 팝업 월은 전용 변수, 섹션 월과 분리 | 공유하면 뒤 화면·버튼 이름이 따라 바뀜 |
| 누계 계산은 태스크 7 로직 재사용 | 두 벌 관리 시 숫자가 어긋남 |
| 인쇄 경로는 태스크 0 후 결정 | 기존 CSS가 열 위치 의존이라 코드를 봐야 함 |
| 신규 SQL 없음 | 기존 조회만 월 인자를 바꿔 재사용 |
