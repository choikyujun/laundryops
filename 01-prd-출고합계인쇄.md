# PRD — 출고 대조 화면 재배치 + 월 누계 인쇄

> 작성: planner (CEgo개발팀 1단계) · 승인 2026-09-11
> 대상 제품: LaundryOps v38 (거래처 파트너 > 출고 대조 섹션)
> 상태: 확정 — architect 핸드오프 완료

---

## 1. 문제와 타깃

- **누구:** 거래처(호텔) 담당자
- **문제:** 매일 보는 건 일자별 내역인데 월 누계 대조표가 위에 있어 스크롤을 내려야 한다. 그리고 월 단위로 세탁공장과 맞춰볼 때 종이로 뽑을 방법이 없다.
- **왜 지금:** 대조 짝짓기가 `invoice_id` 기준으로 정리되어 숫자가 신뢰할 만해졌다.

## 2. 핵심 가치

> 매일 쓰는 화면은 바로 보이고, 월 단위 확인은 따로 열어 인쇄한다.

## 3. MVP 필수 기능

### 3-1. 배치 변경
- **일자별 내역**을 "오늘 출고 수정/입력" 버튼 바로 아래로 이동
- **월 누계 대조표**는 섹션에서 제거하고 팝업으로 이동

### 3-2. 월 합계 버튼
- 위치: "오늘 출고 입력" 버튼과 **같은 행 오른쪽 끝**
- 이름: `10월 합계` — 현재 보고 있는 월 기준으로 동적
- 클릭 시 월 누계 대조표 팝업

### 3-3. 월 누계 팝업
- **월 누계 대조표만.** 일자별 내역 없음
- 팝업 안에서 **월 선택 가능** (◀ ▶ 또는 선택)
- **팝업에서 월을 바꿔도 뒤 화면은 그대로.** 버튼 이름도 안 바뀜
- 인쇄 버튼

### 3-4. 인쇄
- 월 누계 대조표 한 장
- 머리말: 거래처명 · 대상 월 · 출력일
- 상태 표시(세탁 대기, 출고 미입력, 허용 오차)는 인쇄물에도 유지

## 4. 안 만들 것 (Non-goals)

- **일자별 내역 인쇄** — (이번 범위는 월 누계만)
- **엑셀 다운로드** — (인쇄로 충분, v2)
- **팝업↔뒤 화면 월 동기화** — (팝업은 임시 확인 작업. 뒤 화면 맥락 유지가 우선)
- **여러 달 한 번에 인쇄** — (한 달씩)
- **공장 화면 반영** — (이번은 거래처 파트너 화면만)

## 5. 완료 정의 (Acceptance criteria)

**기능**
- [ ] 일자별 내역이 출고 입력 버튼 바로 아래에 온다
- [ ] 월 누계 대조표가 섹션에서 사라지고 팝업에서만 보인다
- [ ] 버튼이 출고 입력 버튼과 같은 행 오른쪽 끝에 있고, 이름이 현재 월을 따른다
- [ ] 팝업에서 월을 바꾸면 표가 갱신된다
- [ ] 팝업에서 월을 바꿔도 뒤 화면과 버튼 이름은 그대로다
- [ ] 인쇄 시 거래처명·대상 월·출력일이 나온다
- [ ] 인쇄물의 금액·수량 정렬이 어긋나지 않는다

**회귀**
- [ ] 출고 입력·수정이 그대로 동작한다
- [ ] 일자별 내역의 행 클릭 펼침이 그대로 동작한다
- [ ] 누계 숫자가 이동 전과 같다
- [ ] 출고 입력 미사용·정액제 거래처에서 섹션이 안 뜬다
- [ ] 폰에서 버튼이 같은 행에 들어간다

**UX**
- [ ] 이모지 없음
- [ ] 안드로이드 크롬 확인

## 6. 재활용 기회

- 태스크 7에서 정리한 "출고가 묶인 명세서" 단위 누계 계산 로직 — 월만 바꿔 그대로 호출
- 앱 내 기존 모달 패턴 — 새로 발명하지 말 것
- 기존 대조 화면 색·배지 규칙 — 팝업과 인쇄물에 동일 적용

## 7. 위험 / 타당성 플래그 (architect 확인)

- **기존 인쇄 경로 재사용 여부** — `printReport()`를 `print-fix.js`가 오버라이드하며, 그 인쇄 CSS가 `th:nth-child(2),(3),(4)` 같은 **열 위치에 의존**한다. 대조표는 열 구성이 달라 그대로 쓰면 정렬이 어긋난다. 재사용할지 별도 인쇄 경로를 만들지 코드 확인 후 결정
- **`_buildHTML()` 통째 교체 방식** — 섹션이 `innerHTML`을 한 번에 갈아끼운다. 순서 변경은 쉽지만 팝업 분리 시 누계 빌드 함수를 따로 떼야 한다
- **월 선택 상태 격리** — 팝업이 뒤 화면의 현재 월 변수를 공유하면 같이 바뀐다. 팝업 전용 상태로 둘 것
- **버튼 한 행 배치** — 폰 좁은 폭에서 버튼 두 개가 줄바꿈되지 않는지
- **누계 계산 재사용** — 태스크 7 로직 그대로. 새로 짜지 말 것

## 8. 성공 지표

월 누계 인쇄를 1회 이상 사용한 거래처 비율.
